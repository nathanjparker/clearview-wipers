# Copy this file to .env.local and fill in the values below.
# Required for Firebase: at least API_KEY and PROJECT_ID (app uses demo mode otherwise).
# For dev: restart "npm run dev" after editing .env.local.
# For deployment (e.g. Vercel): add these same variables in the host's Environment Variables, then redeploy.
#
# Firebase (get these from Firebase Console → Project settings → Your apps → Web app)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# PIN to unlock admin area when not using Firebase login (client-visible; change in production)
NEXT_PUBLIC_ADMIN_PIN=1313
